<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ys.transaction.Infrastructure.dao.YsRechargeOrderDao">

    <resultMap id="YsRechargeOrderMap" type="org.ys.transaction.Infrastructure.pojo.YsRechargeOrder">
        <result property="id" column="id"/>
        <result property="rechargeNo" column="recharge_no"/>
        <result property="userId" column="user_id"/>
        <result property="channel" column="channel"/>
        <result property="amount" column="amount"/>
        <result property="status" column="status"/>
        <result property="tradeNo" column="trade_no"/>
        <result property="payContentType" column="pay_content_type"/>
        <result property="payContent" column="pay_content"/>
        <result property="createdAt" column="created_at"/>
        <result property="paidAt" column="paid_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert">
        insert into ys_tb.ys_recharge_order (
            recharge_no, user_id, channel, amount, status, pay_content_type, pay_content, created_at, updated_at
        ) values (
            #{entity.rechargeNo}, #{entity.userId}, #{entity.channel}, #{entity.amount}, #{entity.status},
            #{entity.payContentType}, #{entity.payContent}, now(), now()
        )
    </insert>

    <select id="selectByRechargeNo" resultMap="YsRechargeOrderMap">
        select * from ys_tb.ys_recharge_order where recharge_no = #{rechargeNo} limit 1
    </select>

    <update id="markSuccess">
        update ys_tb.ys_recharge_order
        set status = 'SUCCESS',
            trade_no = #{tradeNo},
            paid_at = now(),
            updated_at = now()
        where recharge_no = #{rechargeNo}
          and status = 'PENDING'
    </update>
</mapper>
